import { snippy } from './snippy';

describe('Snippy Client Tests', () => {
  test('Then collection should contain correct collection reference', async () => {
    // const content = await snippy({
    //   repos: [['nitrictech/snippet-spike', 'ts']],
    // }).parse();
    // expect(content).toEqual('customers');
  });
});
